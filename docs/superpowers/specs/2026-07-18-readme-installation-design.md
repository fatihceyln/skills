# README Installation Design

## Goal

Add a concise repository README that shows how to install `kiss` with `npx skills`.

## Content

- Identify the repository as a collection of personal Agent Skills.
- State that `kiss` gives brief, direct explanations in at most two sentences.
- Provide an interactive installer command for `kiss`.
- Provide a non-interactive global installation command targeting Codex, Cursor, and Claude Code.

## Scope

Create only `README.md`. Do not add badges, troubleshooting content, or additional skill documentation.
