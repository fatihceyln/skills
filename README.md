Personal Agent Skills.

## KISS (Keep It Stupid Simple)

Give brief, direct explanations in at most two sentences.

### Installation Guide

Install it globally for Codex, Cursor, and Claude Code:

```bash
npx skills add git@github.com:fatihceyln/skills.git --skill kiss --global \
  --agent codex --agent cursor --agent claude-code --yes
```
